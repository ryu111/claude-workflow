---
name: resume
description: 接手並繼續執行 OpenSpec 工作（單步模式）。觸發詞：接手
argument-hint: "<change-id> - OpenSpec 識別碼"
user-invocable: false
disable-model-invocation: true
allowed-tools:
  - Read
  - Glob
  - Grep
  - Write
  - Edit
  - Task
  - Bash
  - TaskList
  - TaskGet
  - TaskUpdate
  - TaskCreate
---

# Resume Command（接手指令）

接手並繼續執行指定的 OpenSpec 工作。**單步模式** - 執行一個任務後停止。

## 使用方式

```
接手 add-user-auth
接手 payment-system
```

## 與 /loop 的差異

| 指令 | 模式 | 說明 |
|------|------|------|
| 「接手」 | 單步 | 執行一個任務後停止，適合需要人工確認的情況 |
| `/loop` | 持續 | 自動執行直到完成，適合批量處理 |

## 執行步驟

### 1. 檢查 OpenSpec 位置

```
openspec/specs/{change-id}/    → 移動到 changes/（表示開始執行）
openspec/changes/{change-id}/  → 直接讀取繼續執行
openspec/archive/{change-id}/  → 提示已完成
```

### 2. 分析任務狀態

```
讀取 tasks.md
├── 統計已完成任務 [x]
├── 找到下一個待處理 [ ]
└── 計算完成百分比
```

### 3. 執行 D→R→T 流程

```
判斷風險等級
├── 🟢 LOW  → D → 完成
├── 🟡 MEDIUM → D → R → T
└── 🔴 HIGH → D → R(opus) → T(完整)
```

### 4. 同步 TaskList（雙向同步）

**啟動時：tasks.md → TaskList**
```
讀取 tasks.md 中所有任務
├── 對每個 [ ] 任務 → TaskCreate
├── 對每個 [x] 任務 → TaskCreate + TaskUpdate(completed)
└── 建立任務依賴關係
```

**任務完成時：TaskList → tasks.md**
```
TaskUpdate(status: completed)
├── 同步更新 tasks.md（[ ] → [x]）
├── 更新 Progress 區塊
└── git commit（如有程式碼變更）
```

### 5. 更新進度

```
更新 tasks.md（標記 [x]）
更新 TaskList（status: completed）
git commit（如有程式碼變更）
顯示下一步提示
```

## 進度顯示

```
╔════════════════════════════════════════════════════════════════╗
║                📊 執行進度: add-user-auth                      ║
╚════════════════════════════════════════════════════════════════╝

├── 總任務：8
├── 已完成：3 (37.5%)
├── 進行中：1
└── 待處理：4

┌─────────────────────────────────────────────────────────────────┐
│ 🔄 當前任務: 1.4 建立 AuthService                              │
│ ├── Phase: 1. Foundation (sequential)                          │
│ ├── 風險: 🟡 MEDIUM → D→R→T                                    │
│ └── 預計檔案: src/services/auth.ts                             │
└─────────────────────────────────────────────────────────────────┘
```

## 並行執行

如果 Phase 是 `(parallel)`，會同時啟動多個 Agent：

```
## ⚡ 並行啟動 3 個 DEVELOPER

Task 2.1: 建立 UserService    → DEVELOPER #1
Task 2.2: 建立 AuthService    → DEVELOPER #2
Task 2.3: 建立 PaymentService → DEVELOPER #3

等待全部完成後，進入 REVIEWER 階段
```

## 任務完成後

```
✅ 任務 1.4 完成！

📊 進度更新: 4/8 (50%)

📋 下一步選項：
├── 接手 add-user-auth  - 繼續下一個任務
├── /loop add-user-auth    - 自動完成剩餘任務
└── 手動檢查後再繼續
```

## OpenSpec 生命週期

```
/plan → specs/          # 建立規格（待審核）
         ↓
接手 → changes/      # 開始執行（進行中）
         ↓
完成 → archive/         # 歸檔（已完成）
```

## 錯誤處理

| 情況 | 處理 |
|------|------|
| change-id 不存在 | 列出可用的 OpenSpec，或提示使用 `/plan` |
| change-id 在 `archive/` | 提示已完成，詢問是否重新開始 |
| tasks.md 格式錯誤 | 嘗試自動修復，或提示手動修正 |
| 所有任務已完成 | 提示歸檔並顯示完成報告 |
| D→R→T 流程被阻擋 | 顯示阻擋原因，引導正確的下一步 |
| REVIEWER REJECT | 自動委派 DEVELOPER 修復 |
| TESTER FAIL | 委派 DEBUGGER 分析 → DEVELOPER 修復 |

## 口語觸發

以下說法都會觸發此指令：

```
"接手 add-user-auth"
"繼續執行 payment-system"
"恢復之前的工作"
```

## Task 雙向同步機制

### 啟動時同步（tasks.md → TaskList）

當執行「接手」時，**必須**將 tasks.md 同步到 TaskList：

```javascript
// 1. 讀取 tasks.md
const tasks = parseTasksMd(tasksContent);

// 2. 對每個任務建立 TaskCreate
for (const task of tasks) {
  TaskCreate({
    subject: `Task ${task.id}: ${task.name}`,
    description: `Agent: ${task.agent}\nFiles: ${task.files}`,
    activeForm: `處理 Task ${task.id}`
  });
}

// 3. 已完成的任務標記為 completed
for (const task of tasks.filter(t => t.completed)) {
  TaskUpdate({ taskId: task.id, status: "completed" });
}
```

### 任務完成時同步（TaskList ↔ tasks.md）

當 D→R→T 流程完成時，**雙向更新**：

```javascript
// 1. 更新 TaskList
TaskUpdate({
  taskId: currentTask.id,
  status: "completed"
});

// 2. 更新 tasks.md（使用 Edit 工具）
// 將 "- [ ] 1.1 任務名稱" 改為 "- [x] 1.1 任務名稱"

// 3. 更新 Progress 區塊
// Completed: N → Completed: N+1
```

### 同步時機

| 時機 | 方向 | 動作 |
|------|------|------|
| 「接手」啟動 | tasks.md → TaskList | 建立所有任務 |
| 任務開始 | TaskList | `status: in_progress` |
| D 完成 | TaskList | `activeForm: "審查中"` |
| R 完成 | TaskList | `activeForm: "測試中"` |
| T PASS | 雙向 | `completed` + 勾選 checkbox |
| T FAIL | TaskList | `activeForm: "除錯中"` |

### 範例

**tasks.md 內容**：
```markdown
## Progress
- Total: 3 tasks
- Completed: 1
- Status: IN_PROGRESS

## 1. Setup (sequential)
- [x] 1.1 初始化專案 | agent: developer | files: package.json
- [ ] 1.2 建立資料庫 | agent: developer | files: src/db/
- [ ] 1.3 設定 Auth | agent: developer | files: src/auth/
```

**對應 TaskList**：
```
ID: 1 | Task 1.1: 初始化專案 | ✅ completed
ID: 2 | Task 1.2: 建立資料庫 | ⏳ pending → 🔄 in_progress
ID: 3 | Task 1.3: 設定 Auth | ⏳ pending
```

---

## 提示

- 「接手」適合需要人工確認每個任務的情況
- 如果想自動完成所有任務，使用 `/loop`
- 每個任務完成後會自動 git commit
- 可以隨時中斷，下次用「接手」繼續
- 所有任務完成後會提示歸檔到 `archive/`
- **TaskList 與 tasks.md 保持同步**，可從任一處查看進度
